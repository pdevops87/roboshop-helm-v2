apiVersion: v1
kind: ConfigMap
metadata:
  name: mychart-configmap
data:
  myvalue: "Hello World"

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap-01
data:
  myvalue: "Hello World"

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap-02
data:
  name: "{{ .Values.name }}"
  chartName: "{{ .Chart.Name }}"
  chartVersion: "{{ .Chart.Version }}"

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap-03
data:
  data.conf: |-
{{ .Files.Get "files/data.conf" | indent 4 }}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap-04
data:
  name: "{{ .Values.name }}"
  chartName: {{ .Chart.Name | quote }}
  chartVersion: {{ .Chart.Version | quote }}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap-05
data:
  name: "{{ .Values.name }}"
  food: {{ .Values.favourite.food }}
  drink: {{ .Values.favourite.drink }}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap
data:
  name: "{{ .Values.name }}"
  food: {{ .Values.favourite.food }}
  drink: {{ .Values.favourite.drink | default "tea" | quote }}